const domains = (state={domains_loaded: []}, action: any) => {
    switch (action.type){
        
        default : state={...state}
                       
    }
    return state;
};

export default domains;